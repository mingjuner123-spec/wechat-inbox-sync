# WeChat Inbox Sync 1.3.74 代码地图

## 基线

- 提交：`f08429ed`
- 正式业务锚点：`4405dcef`
- 插件版本：`1.3.74`
- 发布源：`obsidian-plugin/wechat-inbox-sync/`
- `main.js`：约 20,206 行
- `tests/plugin-main-ai.test.js`：约 13,647 行
- 基线 `main.js` SHA-256：`AB3C12E754E40C55C5BF3C5E30B5C32E55F07600EB9FB6CD8BAD08A90437D91A`
- 核心、市场包、候选、身份和发布治理基线测试：全绿

## 当前职责

| 区域 | 职责 | 风险 |
| --- | --- | --- |
| 本地组件 | ASR/OCR 安装、兼容、日志、修复、运行 | 高，跨 Windows/macOS、文件系统、进程和 CDN |
| 设置与传输 | 设置合并、绑定、Pro、HTTP、下载和中止 | 高，关联双环境和现有用户设置 |
| 转写协议 | 腾讯、阿里、豆包请求/响应、媒体与质量判断 | 中高 |
| 文档转换 | Markdown、AI 元数据、PDF、DOCX、附件 | 中高 |
| 社媒解析 | 小红书、抖音、B站、小宇宙、视频号 | 很高，外部页面和登录态经常变化 |
| 浏览器与 OAuth | Electron 会话、隐藏窗口、飞书、小红书登录 | 很高 |
| 存储与同步 | Vault、frontmatter、附件、队列、失败/删除 | 很高，决定用户文件和幂等性 |
| UI | Modal、进度提示、设置页 | 中高 |

## 主要热点

- `hydrateWebpageMarkdown`：约 850 行。
- 小红书 Electron 渲染：约 780 行。
- 设置页 `display`：约 580 行。
- `runLocalTranscription`：约 177 行。
- `syncBinding`：约 127 行。

## 重要状态和副作用

- 模块级可变状态：小红书浏览器会话队列。
- 插件实例状态：settings、安装 Promise、中止控制器、子进程、当前转写上下文、待删除记录、同步 single-flight、Notice。
- 外部副作用：文件系统、子进程、HTTP、Obsidian `requestUrl`、Electron session/BrowserWindow、Vault adapter、`saveData`、计时器。

## 已确认不能直接复用的文件

`plugin-core.js`、`sync-core.js`、`cloud-client.js` 当前未被插件入口引用，且默认 API/设置已与 1.3.74 漂移。它们是历史参考，不是可直接接回的模块。

## 第一阶段模块

| 顺序 | 模块 | 状态 | 备注 |
| ---: | --- | --- | --- |
| 0 | 确定性构建地基 | 已完成 | `src/main.js` 与正式 `main.js` 基线字节一致；CI/Release 已加漂移门禁 |
| 1 | 日期工具 | 已完成 | UTC+8、跨日和无效日期回退测试通过 |
| 2 | 转写质量 | 已完成 | 固定阈值、提示词泄漏和重复句错误契约 |
| 3 | 进度提示 | 已完成 | 固定文字、计数、ASR 进度和心跳契约 |
| 4 | AI 元数据错误 | 已完成 | 固定分类、注释和用户告警 |
| 5 | 诊断脱敏 | 已完成 | 不扩大或缩小现有脱敏 |
| 6 | Vault 路径 | 已完成 | 不改变目录和回退值 |
| 7 | 输入规范化 | 已完成 | 不改变设置默认值 |
| 8 | 记录元数据 | 已完成 | 固定优先级、清理和补全契约 |
| 9 | 记录状态 | 已完成，待提交检查点 | 高风险，状态矩阵已锁定 |
| 10 | 记录身份 | 待开始 | 高风险，重复笔记差分 |

## 检查点记录

### 构建地基

- `src/main.js` 与 1.3.74 正式 `main.js` SHA-256 均为 `AB3C12E754E40C55C5BF3C5E30B5C32E55F07600EB9FB6CD8BAD08A90437D91A`。
- 固定 esbuild `0.28.1`；当前无本地模块时保持源码字节，出现模块后生成单文件 bundle。
- 构建器拒绝缺失静态依赖、动态 require 和 bundle 中残留的相对 require。
- Linux/Windows 主门禁和 Release 在候选生成前安装固定工具链并校验源码/生成物。
- 核心、市场包、候选、身份和治理回归全绿。
- 独立复审：P0=0、P1=0、P2=0。

### 日期工具

- 从当前 1.3.74 抽离 `pad2`、`getChinaTimeParts`、`getDateFolderName`、`formatCreatedTime`、`getTitleTimePart`。
- UTC+8 跨日、时间戳输入、无效日期回退和生成 bundle 对照均通过。
- `plugin-main-ai` 继续加载最终 `main.js` 做运行行为回归；仅源码结构断言改读 `src/main.js`。
- 市场包测试继续读取最终生成物。
- 根 `main.js` 与权威生成 `main.js` 哈希一致。
- 完整相关回归通过；独立复审 P0=0、P1=0、P2=0。

### 转写质量

- 从当前 1.3.74 抽离重复行去除、质量单元归一化、提示词泄漏/重复句检测和错误构造六个纯函数。
- 新模块与原内联实现逐项等价；调用点和 `PluginClass.__test` 导出保持不变。
- 定向模块测试覆盖正常文本、相邻重复、提示词泄漏、重复句循环、空结果和最终生成 bundle。
- 构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 小红书、ASR/OCR 安装、同步/暂停删除、设置、版本和 Release Pipeline V2 均未改。
- 独立复审未发现 P0/P1/P2。

### 进度提示

- 从当前 1.3.74 抽离同步结果、跳过原因、转换告警、进度百分比、ASR 日志、耗时和心跳提示十个纯函数。
- 完整保留 `locally-quarantined-unrecoverable` 与 `deleted-expired-xhs-shortlink` 的 1.3.74 提示、计数和排除规则。
- 定向测试同时验证源模块、最终生成 `main.js` 和 `PluginClass.__test`。
- 构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 小红书提取、ASR/OCR 安装、同步 single-flight/暂停删除、设置、版本和 Release Pipeline V2 均未改。
- 独立复审：P0=0、P1=0、P2=0。

### AI 元数据错误

- 从当前 1.3.74 抽离 AI 错误分类、HTML 诊断 marker 和用户告警三个纯函数。
- 429、5xx、timeout、empty 和默认分类，以及五类中文提示保持逐项等价。
- 错误分类写入、Markdown marker、同步成功但 AI 元数据失败提示三处调用均保留。
- 定向模块、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 冻结区和 Release Pipeline V2 均未改；独立复审 P0=0、P1=0、P2=0。

### 诊断脱敏

- 从当前 1.3.74 抽离嵌套对象敏感键脱敏和已知凭据文本替换两个纯函数。
- 主 token、兑换码、权益 code、bindingToken 和各绑定 token 来源均保留。
- 保持长凭据优先、大小写敏感键识别、数组递归、普通字段与空输入行为不变。
- 定向模块、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 冻结区和 Release Pipeline V2 均未改；独立复审 P0=0、P1=0、P2=0。

### Vault 路径

- 从当前 1.3.74 抽离 Vault 路径规范化、配置路径回退和设置持久化判断三个纯函数。
- 模块默认值 `临时收集` 与 1.3.74 的 `DEFAULT_SETTINGS.inboxDir` 严格一致。
- 空值、绝对路径、盘符、UNC、NUL、`.` / `..` 路径段和非法 fallback 行为保持不变。
- 定向模块、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 冻结区和 Release Pipeline V2 均未改；独立复审 P0=0、P1=0、P2=0。

### 输入规范化

- 从当前 1.3.74 抽离笔记保存模式、属性字段和绑定码规范化三个纯函数。
- 主入口 wrapper 保持原单参数调用和 `__test` 契约。
- 保存模式原型键、字段白名单/去重/顺序、ASCII 与 Unicode 横线及六位绑定码行为保持不变。
- 定向模块、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 冻结区和 Release Pipeline V2 均未改；独立复审 P0=0、P1=0、P2=0。

### 记录元数据

- 从当前 1.3.74 抽离作者、描述、关键词、Markdown 描述清理、关键词提取和网页元数据补全六个纯函数。
- 字段优先级、数组引用、字符串拆分、候选词顺序/上限和 fallback 上限均保持不变。
- 元数据补全继续浅复制输入，不覆盖已有 description/keywords。
- 定向模块、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 冻结区和 Release Pipeline V2 均未改；独立复审 P0=0、P1=0、P2=0。

### 记录状态

- 仅抽离“云端转写等待”和“音视频转写未完成”两个纯布尔判断。
- 云端来源、状态集合、已有转写/Markdown、voice/audioFileID/transcriptOnly 条件逐项保持 1.3.74 行为。
- 同步调用位置与判断顺序未移动；跳过、删除、重试和同步编排均未改。
- 定向状态矩阵、构建、插件核心、市场包、候选、发布身份、治理、镜像和语法检查均通过。
- 独立复审 P0=0、P1=0、P2=0。

## 冻结区

- 小红书全链。
- ASR/OCR 全链。
- 暂停/删除、同步 single-flight 和失败清理。
- 双环境 API、绑定、Pro、飞书 OAuth。
- 设置 UI。
- manifest、versions、运行标记和发布候选契约；根 `main.js` 只允许跟随权威生成物单向同步。

## 下一阶段候选

只有第一阶段完成并通过用户本地测试后，才重新评估：

1. Frontmatter 生成器。
2. 附件和 Vault 存储接口。
3. 同步状态机。
4. 飞书纯内容解析。
5. PDF/DOCX 纯转换。
6. 平台解析器接口。

## 第一阶段完成记录：记录身份

- 从当前 1.3.74 原样抽离 frontmatter、隐藏记录标记和 URL 身份比较共 10 个纯函数。
- 保持 BOM、LF/CRLF、字段大小写、单双引号、记录 ID 优先级、URL hash/末尾斜杠及 query 顺序语义不变。
- 重复笔记判断的调用位置与“记录 ID 优先、URL 兜底”顺序未移动。
- 定向模块、构建、插件核心、市场包、候选包、发布身份、发布治理、镜像和语法检查全部通过。
- 独立复审：P0=0、P1=0、P2=0。
- 第一阶段 10 个低耦合模块均已完成，下一步进入整条本地架构分支终审与本地测试候选交付。

## 第一阶段本地交付状态

- `src/main.js` 已降至约 19,681 行；10 个低耦合模块共约 705 行，模块职责和测试可独立定位。
- 最终生成的单文件 `main.js` 仍满足 Obsidian 市场加载约束。
- 双次构建哈希一致，根镜像与权威 `main.js` 一致，1.3.74 版本身份未变。
- 10 个模块测试已进入主分支和 Release 自动门禁，未来遗漏任一项都会失败关闭。
- 整体独立终审最终 P0=0、P1=0、P2=0。
- 已生成并验证本地候选 `1.3.74-a1e0dc0502c7029b`；当前等待负责人本地核心功能验收，不发布。

## 第二阶段本地候选：笔记输出边界

- 基线采用正式 1.3.75 的发布提交；本段仍沿用本代码地图文件名，仅记录本地候选，不改变任何版本身份。
- 新增 `src/note-output-plan-utils.js`：唯一负责 frontmatter、Markdown、隐藏诊断标记与最终笔记路径的纯计算。
- `writeRecord` 继续负责目录、附件、网页处理、AI、进度、Vault 写入和同步状态；它只在真实写入前调用输出计划，写入顺序不变。
- 共享的 `getRecordUrl` 和 `getRecordSourceLabel` 保留在主入口，因为同步去重、失败诊断与笔记输出共同依赖它们；未为了拆分而重复实现或移动副作用。
- 本地回归覆盖文本、链接、网页、语音、文件、自定义字段、隐藏记录/AI 错误标记、根目录和按日期目录路径；没有发布、部署或替换用户插件。

## 第三阶段本地候选：内容正文格式化

- 基线继承第二阶段本地候选，继续保持 1.3.75 版本身份不变。
- 新增 `src/record-body-markdown-utils.js`：唯一负责网页、文件、音视频转写的正文 Markdown 与转写元数据的纯计算。
- `src/main.js` 仍负责解析、下载、AI、目录、Vault 写入、同步状态和所有网络/文件系统副作用；第二阶段的 `note-output-plan-utils` 仅继续接收同名正文函数。
- 六类正文回归覆盖普通网页、飞书网页、视频号未接通提示、转写网页、转写文件和普通文件；输入 record 不得被改写。
- 本地候选尚未发布、部署、上传或替换用户插件；待独立审查后与其他本地拆分一并手工测试。

## 第四阶段本地候选：AI 元数据纯格式化

- 基线继承第三阶段本地候选，继续保持 1.3.75 版本身份不变。
- 新增 `src/ai-metadata-utils.js`：唯一负责关键词规范化、模型返回解析、结果规范化和 AI 输入文本提取四项纯计算。
- `src/main.js` 仍负责是否触发 AI、实际模型请求、429/超时处理、同步、网页解析、ASR/OCR、附件与 Vault 写入；`cleanMarkdownForStorage`、`stripMarkdownCodeBlocks` 和 `tryParseJson` 仍由主入口以显式依赖提供。
- 回归测试直接读取第三阶段历史生成物 `1a9c8d6e`，比较数组/字符串关键词、JSON/fenced JSON/标签式/空响应、结果截断、网页与转写输入文本，并断言输入记录不变。
- 本地候选尚未发布、部署、上传或替换用户插件；待完整本地验收和独立审查后与其他本地拆分一并手工测试。
