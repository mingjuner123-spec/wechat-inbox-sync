# 插件 1.3.56 实施与发布计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将停止转写入口常驻补丁与 `1.3.55` 已包含的 macOS/Windows ASR 修复统一发布为 `1.3.56`。

**Architecture:** 从实时公开默认分支建立干净候选，只精确移植一个小补丁；版本元数据、构建资产、标签和 Release 绑定同一提交；通过 prepublish/postpublish 身份门禁防止“Release 已建但市场默认分支仍旧”的错误。

**Tech Stack:** Obsidian JavaScript 插件、Node 回归测试、PowerShell/Bash 安装器、GitHub Release。

---

### Task 1: 固定发布工具与停止入口补丁

**Files:**
- Create: `scripts/plugin-release-identity-core.js`
- Create: `scripts/check-plugin-release-identity.js`
- Create: `tests/plugin-release-identity.test.js`
- Modify: `.github/workflows/release.yml`
- Modify: `obsidian-plugin/wechat-inbox-sync/main.js`
- Modify: `tests/plugin-main-ai.test.js`

- [ ] 先为公开仓库自包含的发布身份门禁写失败测试；生产入口只依赖 Node 内置模块、Git、GitHub 固定 API，不依赖综合项目私有 Harness，也不开放测试命令注入。
- [ ] 纯函数测试覆盖畸形输出、近似 404、四份元数据漂移、tag/main/Release SHA 漂移、资产缺失和超时；临时 bare Git 集成测试覆盖 clean/dirty、远端 main 前移、annotated tag peeling 与 pre/postpublish 状态。
- [ ] 复用公开仓库现有 `release-source-guard-core.js` 的 Git 身份规则，新增严格解析/验证核心与 CLI，使网络错误、解析失败和非精确 HTTP 404 一律失败关闭。
- [ ] 修改 Release 工作流：若同版本 Release 已存在立即失败，只允许首次 `gh release create`，禁止 `upload --clobber`；用回归测试固定该约束。
- [ ] 精确重放 `02734f8` 的 `+6/-1` 停止入口常驻补丁，不手工携带其他候选。
- [ ] 运行 `plugin-main-ai`、`plugin-marketplace-package`、`release-governance`、发布身份测试和 `git diff --check`。

### Task 2: 创建 1.3.56 原子候选

**Files:**
- Modify: `manifest.json`
- Modify: `versions.json`
- Modify: `obsidian-plugin/wechat-inbox-sync/manifest.json`
- Modify: `obsidian-plugin/wechat-inbox-sync/versions.json`
- Create: `output/wechat-inbox-sync-1.3.56.zip`（不进入 Git）

- [ ] 同一提交把两份 manifest 升到 `1.3.56`，两份 versions 加入 `"1.3.56": "1.0.0"` 并保持逐字一致。
- [ ] 本地 ZIP 仅作为预检产物，不进入 Git、不作为正式发布身份；正式 ZIP 必须由 GitHub 工作流从最终 tag checkout 构建，包含 `main.js`、`manifest.json`、`styles.css`、`versions.json`、`README.md`、`LICENSE`、`local-asr/`、`local-ocr/`。
- [ ] 运行插件全量回归、安装器语法、组件 manifest/CDN 和资产哈希检查。
- [ ] 独立规格审稿和代码质量审稿通过后提交候选。

### Task 3: 合并、发布和回读

- [ ] 推送 `codex/release-plugin-1.3.56`，通过受保护分支流程把候选合并到 `main`。
- [ ] 合并后以最终 `origin/main` SHA 为唯一发布候选；同步本地发布 Worktree 到该 SHA，确认工作区干净，再创建指向该 SHA 的本地 annotated tag `1.3.56`。
- [ ] 在尚未推送标签时运行 prepublish：远端精确 tag 必须为空、固定 Release endpoint 必须返回精确 HTTP 404，四份版本元数据和本地 annotated tag peeled SHA 必须与最终 `origin/main` SHA 一致。
- [ ] prepublish 通过后才推送精确标签 `1.3.56`，由 tag checkout 的发布工作流首次创建包含 `main.js`、`manifest.json`、`styles.css`、`versions.json` 与 ZIP 的正式 Release；任何已存在同版本 Release 都必须失败，禁止覆盖。
- [ ] 运行 postpublish 身份门禁与 Obsidian 发布检查脚本。
- [ ] postpublish 必须从三次独立 UTC 新鲜采样回读 GitHub 默认分支、tag peeled commit、Release 目标提交和五项独立资产；下载正式 ZIP，验证条目集合及关键文件字节与 tag checkout 一致，再回读 raw manifest 与 Marketplace。
- [ ] 更新发布台账；任何闭环失败不得宣称市场已上线，且不得覆盖 `1.3.56`。
