# 插件 1.3.56 实施与发布计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将停止转写入口常驻补丁与 `1.3.55` 已包含的 macOS/Windows ASR 修复统一发布为 `1.3.56`。

**Architecture:** 从实时公开默认分支建立干净候选，只精确移植一个小补丁；版本元数据、构建资产、标签和 Release 绑定同一提交；通过 prepublish/postpublish 身份门禁防止“Release 已建但市场默认分支仍旧”的错误。

**Tech Stack:** Obsidian JavaScript 插件、Node 回归测试、PowerShell/Bash 安装器、GitHub Release。

---

### Task 1: 固定发布工具与停止入口补丁

**Files:**
- Create: `scripts/check-plugin-release-identity.js`
- Create: `tests/plugin-release-identity.test.js`
- Modify: `obsidian-plugin/wechat-inbox-sync/main.js`
- Modify: `tests/plugin-main-ai.test.js`

- [ ] 先为发布身份门禁写失败测试，覆盖权威仓库/默认分支/版本未占用、prepublish 与 postpublish 闭环状态。
- [ ] 从中央已审计候选精确移植发布身份门禁实现，使测试通过。
- [ ] 精确重放 `02734f8` 的 `+6/-1` 停止入口常驻补丁，不手工携带其他候选。
- [ ] 运行 `plugin-main-ai`、`plugin-marketplace-package`、`release-governance`、发布身份测试和 `git diff --check`。

### Task 2: 创建 1.3.56 原子候选

**Files:**
- Modify: `manifest.json`
- Modify: `versions.json`
- Modify: `obsidian-plugin/wechat-inbox-sync/manifest.json`
- Modify: `obsidian-plugin/wechat-inbox-sync/versions.json`
- Create: `output/wechat-inbox-sync-1.3.56.zip`（不进入 Git）

- [ ] 验证远端不存在 `1.3.56` 标签或 Release。
- [ ] 同一提交把两份 manifest 升到 `1.3.56`，两份 versions 加入 `"1.3.56": "1.0.0"` 并保持逐字一致。
- [ ] 从 `obsidian-plugin/wechat-inbox-sync/` 构建完整 ZIP，包含 `main.js`、`manifest.json`、`styles.css`、`versions.json`、`README.md`、`LICENSE`、`local-asr/`、`local-ocr/`。
- [ ] 运行插件全量回归、安装器语法、组件 manifest/CDN 和资产哈希检查。
- [ ] 独立规格审稿和代码质量审稿通过后提交候选。

### Task 3: 合并、发布和回读

- [ ] 推送 `codex/release-plugin-1.3.56`，通过受保护分支流程把候选合并到 `main`。
- [ ] 确认 `main HEAD` 等于候选提交并再次运行 prepublish 身份门禁。
- [ ] 创建精确标签 `1.3.56`，由发布工作流创建包含 `main.js`、`manifest.json`、`styles.css`、`versions.json` 与 ZIP 的正式 Release。
- [ ] 运行 postpublish 身份门禁与 Obsidian 发布检查脚本。
- [ ] 回读 GitHub 默认分支、tag peeled commit、Release 资产、ZIP 内 manifest、raw manifest 与 Marketplace。
- [ ] 更新发布台账；任何闭环失败不得宣称市场已上线，且不得覆盖 `1.3.56`。
